# flipdeck 进度总表（活文档）

> **这份是入口文档。** 原则：**先把原型做出来，能简化的一律简化**；更好的方案全部记在
> §5「升级待办（以后再说）」，不阻塞原型。
>
> 下面的「当前状态」区块由 `python status.py` 从仓库真实状态生成，其余内容是手写的计划与决策记录。

<!-- AUTO:STATUS:BEGIN -->
### 当前状态（脚本自动生成：2026-09-14 01:47，运行 `python status.py` 刷新）

- 机型：**iPhone 12 / 12 Pro**（146.7 × 71.5 × 7.4 mm），已出全套 STL 与渲染
- 打印件：**9 件**，✔ 全部水密（边界边 0 / 非流形边 0 才算通过）
- 装配干涉：结论：全部姿态无干涉
- 板框/开关架自检：结论：全部通过 ✔
- 渲染图：`out/ip12/` 共 15 张（09-14 01:32）

**打印件明细（`out/stl/`）**：

| 零件 | 三角面 | 实体体积 cm³ | 水密 | STL |
|---|---|---|---|---|
| deck | 669532 | 129.7 | ✔ | `deck.stl` (33.5MB) |
| lid | 181888 | 31.9 | ✔ | `lid.stl` (9.1MB) |
| cradle | 232348 | 64.7 | ✔ | `cradle.stl` (11.6MB) |
| cap_dpad | 11664 | 2.3 | ✔ | `cap_dpad.stl` (0.6MB) |
| caps_abxy | 10784 | 1.3 | ✔ | `caps_abxy.stl` (0.5MB) |
| caps_small | 3072 | 0.2 | ✔ | `caps_small.stl` (0.2MB) |
| lever_l | 5452 | 0.7 | ✔ | `lever_l.stl` (0.3MB) |
| lever_r | 5452 | 0.7 | ✔ | `lever_r.stl` (0.3MB) |
| switch_frame | 188888 | 18.5 | ✔ | `switch_frame.stl` (9.4MB) |

**文档**：`PROGRESS.md`（本文，进度+计划+决策）、`README.md`（结构/尺寸/物料）、`DIY_GUIDE.md`（打印+装配+分期）、`ELECTRONICS.md`（采购+引脚表+固件）、`out/BUILD_NOTES.md`、`out/clearance_report.md`、`out/pcb/PCB_FIT_REPORT.md`

**一键复现**：`python build.py --render-dir ip12`（出 STL+渲染+报告，约 6 分钟）／`python check_poses.py`（只跑干涉，约 4 分钟）／`python pcb_outline.py`（板框+自检）／`python debug_slices.py`（剖面 ASCII 自检）／`python status.py`（刷新本文状态块）

<!-- AUTO:STATUS:END -->

---

## 1. 现在到哪一步了（一句话）

**结构设计已完成并验证 → 电子方案已定 → 下一步就是买件、打印、动手装配。**

- ✅ 机构（双平行轴：合盖 + 托盘翻转 180°）已定型，位置、间隙、翻转全程都做过数值验证
- ✅ 9 个打印件全部水密（边界边 0 / 非流形边 0），STL 可直接切片
- ✅ 底盘/盖框与机型无关，只有托盘随手机变（你的 iPhone 12 版已出好）
- ✅ 电子方案：**自建路线已定**（ESP32-S3 + 2 摇杆模块 + 11 开关 + 打印开关架），不需要画 PCB
- ✅ **固件已写完**：`firmware/flipdeck_gamepad/flipdeck_gamepad.ino`（+ 烧录/标定说明 `firmware/README.md`）
- ✅ 采购清单、引脚表、装配顺序、分期计划都写好了
- ⬜ 还差：**买件 → 打 0 期首件（只打托盘验手机）→ 焊线 → 刷固件 → 装电子 → 整机**（全是动手的活，没有设计阻塞）

## 2. 原型优先：简化后的最短路径

原型阶段砍掉一切非必需的东西，目标是**先让它动起来、能玩**：

| 期 | 做什么 | 打印 | 花费 | 通过标准 |
|---|---|---|---|---|
| **0** | 只打 `cradle`（11MB，~29g，3–4h） | 1 件 | ¥0 | 手机放进去贴平、相机凸台落进让位坑、挡墙不遮屏幕 |
| **1** | 打 `lid` + `deck` + `hinge_pin`，**0.28mm 层高、15% 填充、不装电子** | 3 件，~75g，6–8h | ¥0 | 托盘能在导轨里转、0°/110°/180° 有"咔"感、合盖顺畅、用纸片模拟控制高度确认不顶手机 |
| **2** | 电子**在桌面裸接**跑通（不装进壳）：主控 + 1 个摇杆 + 4 个键 + **刷固件** | 0 | ~¥70 | iPhone 12 蓝牙能连上手柄，模拟器里摇杆/按键全部响应，延迟可接受 |
| **3** | 电子装进底座（2 个摇杆 + 11 键 + 开关架），整机装配 | `switch_frame` + 键帽 + 杠杆 | +~¥60 | 合盖、翻转、换形态全部顺畅，能连续玩 |
| 4 | 手感调优（棘轮松紧、摇杆帽高、肩键行程） | 小件 | ¥0 | — |

**原型阶段刻意砍掉的东西**（都能后期加回来，见 §5）：
- ❌ 磁铁（MagSafe 环 + 合盖磁铁）→ **手机本来就靠托盘的沉台 + 1.2mm 挡墙卡住，直接放上去就行**，不用胶也不用磁铁（见下）
- ❌ 电池 + 充电板 → 2 期直接插 USB 供电（开发板自带 Type-C），省 ¥30 和一堆走线
- ❌ 泡棉密封条 → 合盖有轻微晃动不影响验证
- ❌ 肩键杠杆（先打也行，不打就把开关线引到后壁孔用指甲按）
- ❌ 完整 PCB → 用打印开关架替代（已出件）

**关于"手机怎么固定"（澄清）**：托盘本身就够用 ——
沉台深 0.8mm、四周挡墙高出手机背面约 1.3mm（游戏位）/ 0.7mm（手机位）、单边间隙 0.35mm。正常拿着玩、开盖、翻转、合盖，重力方向都是把手机压进托盘里，不会掉。
只有两种情况下需要加固：**倒过来甩**、或者**装兜里挤**。到那时再上磁铁（或打印 TPU 卡爪）就行，不影响现在的验证。
所以原型阶段：手机 = 直接放上去，零成本。

### 最短采购清单（原型版，¥60–150）

**首选路线：买一个便宜蓝牙手柄当"供体"拆件（¥50–90）** —— iOS 原生支持标准蓝牙手柄，**不需要 MFi**。拆出它的主板 / 摇杆 / 电池 / 充电口 / 电源开关全部用上，你只要把 11 个开关焊到它的按键矩阵（万用表量行列）。**不用写固件、不用转接头、结构不用改**。

| 件 | 搜索词 | 价 | 必需 |
|---|---|---|---|
| **供体手柄**（推荐） | 便宜蓝牙手柄（外形无所谓，反正是拆件） | ¥50–90 | ✔ 首选 |
| 或：主控 | `ESP32-S3 SuperMini`（想自己写固件走这条） | ¥25–40 | ⭕ 二选一 |
| 或：摇杆 | `碳膜摇杆模块 带排线` ×2（供体的摇杆能用就不用买） | ¥10–30 | ⭕ |
| 开关 | `6x6x2.0 贴片轻触开关` ×11 | ¥5–15 | ✔ |
| 铰链轴 | `M4 螺杆 150mm` + `M4 螺母` + `Ø10x1.0 尼龙垫圈` | ¥10–15 | ✔ |
| 托盘轴 | `M3x16 螺钉` + `M3 方螺母` + `Ø10x0.8 尼龙垫圈` | ¥8–12 | ✔ |
| 螺丝 | `M2 自攻螺丝` 若干（摇杆 8 颗） | ¥5 | ✔ |
| 线 | `30AWG 硅胶线` | ¥10 | ✔ |
| 磁铁 | `Ø8x2 圆磁铁` ×8 + `MagSafe 磁吸环` | ¥15–25 | ⭕ 可延后（托盘挡墙就够） |
| 电池 / 充电 | 软包 ≤60×50×5.5 + `TP4056` | ¥25–35 | ⭕ 供体自带就不用买 |

> **两条路线总价接近**：供体 ¥50–90（要逆向按键矩阵）vs 散件自建 ¥40–70（要刷固件，代码我给）。
> 会动烙铁的话自建更干净；供体路线唯一优势是省掉固件。挑供体三要点：页面明确写"支持 iOS"、真摇杆（不是 8 向假的）、螺丝可拆。详见 `ELECTRONICS.md` §2。
> ⚠️ 供体的摇杆不一定正好塞进 Ø32 口袋 —— 量一下直径/高度告诉我，我改口袋或出转接环。

> **那 ¥100 的手柄测试是测什么**：测的是"**你的游戏/模拟器认不认手柄**"，跟蓝牙还是有线**无关** —— iOS 上游戏对手柄的支持是系统/游戏层面的事，游戏不认手柄，插线也没用。所以：
> - 你**已经有**蓝牙手柄 → 直接拿它试，不用花钱（而且它可能就是你的供体）；
> - 你确定要玩的游戏（比如 Delta 模拟器里那些）本来就支持手柄 → 这步可以直接跳过；
> - 完全不确定 → 买个便宜的试一次，反正拆了当供体也不亏。
>
> **蓝牙 vs 有线**：有线延迟更低（2–8ms vs 10–25ms），但 Lightning 要有线得走相机转接头或拆一个 MFi 手柄取板，而且**换形态必须先拔线**（托盘要转 180°）。所以默认 v1 蓝牙；要低延迟优先，见 `ELECTRONICS.md` §3 —— 那条路我可以把结构改成"短线舱 + 拔插位"，你说一声就改。

## 3. 打印清单（现在就能切）

| 文件 | 件 | 什么时候打 |
|---|---|---|
| `out/stl/cradle.stl` | 托盘（**唯一与机型相关的件**） | 0 期，先打这个 |
| `out/stl/lid.stl` | 盖框 + 铰链凸耳 | 1 期 |
| `out/stl/deck.stl` | 控制底座（控制井/摇杆口袋/接口孔/PCB 柱） | 1 期 |
| `out/stl/hinge_pin.stl` | 销轴（**建议直接用 M4 螺杆，这件可不打**） | ⭕ |
| `out/stl/switch_frame.stl` | **打印开关架（替代 PCB）** | 3 期 |
| `out/stl/caps_abxy.stl` / `cap_dpad.stl` / `caps_small.stl` | 键帽 | 3 期 |
| `out/stl/lever_l.stl` / `lever_r.stl` | 肩键杠杆 | ⭕ 可延后 |

打印参数：PETG，0.4 喷嘴，3 圈壁。首件可用 0.28mm 层高 + 15% 填充快速验证；终版 0.16mm + 30%。打印后务必用 Ø3.2/Ø4.3 钻头、Ø6.3 铰刀过一遍运动副孔。

## 4. 装配顺序（照抄即可）

1. 修孔、去毛刺
2. **电子先在桌面裸接跑通**（别装进壳）
3. 摇杆拧进底座口袋（4×M2）；主控拧在底座 4 个 PCB 柱上
4. 开关压进开关架 → 焊线（每个开关一根信号线 + 公共 GND）→ 开关架拧在底座上 → 扣键帽
5. 合盖铰链：凸耳交错 → 插 M4 螺杆 → 两侧尼龙垫圈 → 螺母拧到"有阻尼不卡"
6. 托盘：轴颈插进导轨轴承孔 → M3 螺钉 + 尼龙垫圈 + 方螺母 → 调到"手拨能转、松手不掉"
7. 合盖、反复换形态 20 次，看有无松动

## 5. 升级待办（以后再说，不阻塞原型）

| 优先级 | 事项 | 说明 |
|---|---|---|
| P1 | 电池 + 充电板内置 | 拔掉 USB 线独立玩 |
| P1 | 加固手机固定 | 倒甩/装兜会滑出来时再加：MagSafe 环压进托盘 + 合盖磁铁 ×8（托盘轴颈旁留屏蔽，避免霍尔漂移），或打印 TPU 卡爪 |
| P1 | 有线低延迟（可选） | 走相机转接头，或拆一个 MFi 手柄取板（真·Lightning 直插）。需要改结构：deck 前端加"短线舱 + 拔插位" |
| P2 | 肩键杠杆 + 后壁开关 | 手感更完整 |
| P2 | 完整 PCB | 板框已生成（`out/pcb/`），要干净/批量时打板 |
| P2 | 硬限位（坡形挡块） | 防止暴力过转，现在只靠凸点+摩擦自锁 |
| P3 | 相机通孔（合盖也能拍照） | 需要托盘轴偏心，结构复杂一档 |
| P3 | 磁吸可拆 deck（瘦身版） | 合盖厚度从 44mm 降到 ~20mm 的唯一办法 |
| P3 | 竖屏 GB 布局变体 | 现在是横屏 3DS 布局，换布局要改 deck + 托盘 |
| P3 | 换机型（Max/Plus 那档） | 手机宽 >157mm 时托盘要放大，会连锁放大盖框和底座 |
| P3 | 固件打磨（摇杆曲线/中心漂移补偿/连发/OTA/多设备切换） | 基础固件已写完，这些都属锦上添花，单项 0.5–4 小时（详见 `firmware/README.md`） |

## 6. 关键决策记录（为什么这么做）

| 决策 | 原因 |
|---|---|
| 两根**平行**轴（θ 合盖 + φ 托盘翻转），轴都沿 X | YZ 平面内是简单连杆：不用万向节、**转轴里一根线都不走** → 可 3D 打印 |
| 托盘轴放在**托盘板中面** | 翻转 180° 后托盘仍占同一层，只有手机从盖内侧换到盖外侧；否则要么盖变厚、要么撞底座 |
| 铰链凸耳**全部移到左右两侧** | 托盘翻转的扫掠圆（半径 37.9mm）内不能有任何盖的材料，中间不能有腹板 |
| 控制井下沉 + 摇杆再沉进口袋 | 合盖时手机屏幕平面就是底座顶面，所有控制件必须低于它（现在净空 3.5mm） |
| 按键簇前移 3mm（y −14 → −17） | 否则 ABXY 上排/十字上臂的开关焊盘会压进 Ø32 摇杆口袋（那里没有板子/开关架可放） |
| 不做 PCB，用打印开关架 | 原型阶段最快：摇杆直接拧底座、主控用现成开发板、11 个开关由打印件定位 |
| 蓝牙而不是有线 | 托盘要转 180°，有线必须过线/拔线；Lightning 有线还要相机转接头或拆 MFi 手柄 |
| 底盘/盖框与机型无关 | 只有托盘（160×74.5×7 接口标准）随手机变；上限 157×71.5mm，Max/Plus 要整体放大 |

## 7. 变更记录

| 日期 | 做了什么 |
|---|---|
| 2026-09-14 | **固件写完**：`firmware/flipdeck_gamepad/flipdeck_gamepad.ino`（BLE HID + 摇杆自动标定/死区/滤波 + 十字键帽子开关 + 电量可选）+ `firmware/README.md`（装环境/烧录/配对/标定/排错）。路线定为**自建**（ESP32-S3，不用拆供体） |
| 2026-09-14 | 澄清手机固定（挡墙+沉台就够，不用胶不用磁铁）与连接方式（蓝牙/有线成本对比：拆蓝牙手柄取板最省，¥60–100 且不用写固件）；采购清单改成"供体手柄优先" |
| 2026-09-14 | 建活文档 + `status.py`；确定"原型优先"路线；原型简化项与升级待办分离 |
| 2026-09-14 | 出打印开关架 `switch_frame.stl`（替代 PCB）；按键簇前移 3mm 修掉与摇杆口袋的冲突；键帽改成嵌进孔内压 2mm 开关；PCB 板框 + 自检 9 项全过 |
| 2026-09-14 | 电子方案文档 `ELECTRONICS.md`（采购/引脚表/固件/Lightning 有线三条路）；`DIY_GUIDE.md` 分期计划 |
| 2026-09-14 | 切换到 iPhone 12；一壳多机边界算清（可装上限 157.0×71.5mm） |
| 2026-09-14 | 间隙检查公式修正（`min max(dA,dB)`）、托盘位姿修正（绕轴心旋转）、密封条槽同心内缩、体素相位自动搜索 → 9 件全水密、13 项姿态检查全过 |
| 2026-09-14 | 机构定型：双平行轴 + 托盘中面轴 + 铰链走两侧；STL/渲染/报告流水线（`build.py`） |

## 8. 文档地图

| 文件 | 内容 |
|---|---|
| `PROGRESS.md` | **本文：进度 + 计划 + 决策 + 待办** |
| `README.md` | 机构原理、关键尺寸、零件表、已验证间隙、目录说明 |
| `DIY_GUIDE.md` | 打印方案、装配顺序、分期计划、风险清单、一壳多机边界 |
| `ELECTRONICS.md` | 采购清单、Lightning 有线三条路、打印开关架用法、引脚表、固件、动手顺序 |
| `firmware/flipdeck_gamepad/flipdeck_gamepad.ino` | **手柄固件**（ESP32-S3，BLE HID，含摇杆自动标定） |
| `firmware/README.md` | 装环境、烧录、配 iPhone、摇杆标定、排错、接线对照 |
| `out/BUILD_NOTES.md` | 最近一次构建：零件网格数据 + 间隙检查结论 |
| `out/clearance_report.md` | 13 项姿态间隙明细 |
| `out/pcb/` | PCB 板框（DXF/SVG/PNG）+ 板级自检报告 |
| `out/ip12/` | iPhone 12 版渲染图（含总览拼图） |
